apiVersion: v1
kind: Service
metadata:
  name: faturamento-service
  namespace: peo-platform
spec:
  selector:
    app: faturamento-api
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
